{
  "compilerOptions": {
    "baseUrl": ".",   // 새로운 폴더로 들어와서 작업
    "paths": {
      "~/*": [      // ~
        "./*"
      ],
      "@/*": [      // @는 src 라는 뜻이 된다.
        "./src/*"
      ],
    }
  },
  "exclude": [    // 컴파일 되지 않는 폴더들
    "node_modules",
    "dist"
  ]
}